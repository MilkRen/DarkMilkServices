<Window x:Class="LauncherDM.Views.Windows.AuthorizationWIndow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LauncherDM.Views.Windows"
        mc:Ignorable="d"
        Title="AuthorizationWIndow" 
        Height="350" Width="600"
        AllowsTransparency="True" Background="Transparent" ResizeMode="NoResize" 
        WindowStartupLocation="CenterScreen" WindowStyle="None">
    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="50"/>
            <RowDefinition/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.RowSpan="5" BorderBrush="White" Opacity="0.5" Margin="1" BorderThickness="1" />
        <Image />

        <Grid Grid.Row="0">
            <!--<StackPanel Orientation="Horizontal" HorizontalAlignment="Right" >

                <Button x:Name="MinimizeBtn" Background="Transparent" BorderThickness="0" Margin="0,0,0,0" Width="30">
                    <Image Source="/Source/LoadingPage/Minimize_1.png" Height="15" Width="15">
                        <Image.Effect>
                            <BlurEffect Radius="1"/>
                        </Image.Effect>
                    </Image>

                </Button>

                <Button x:Name="CloseBtn" BorderThickness="0" Margin="0,0,0,0"  Width="30" >
                    <Button.Style>
                        <Style TargetType="{x:Type Button}">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="DarkRed"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>

                    <Image x:Name="ImgCloseBtn" Height="15" Width="15">
                        <Image.Style>
                            <Style TargetType="{x:Type Image}">
                                <Setter Property="Source" Value="/Source/LoadingPage/Close_3.png"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMouseOver, ElementName=ImgCloseBtn}" Value="True">
                                        <Setter Property="Source" Value="/Source/LoadingPage/Close_1.png"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Image.Style>
                        <Image.Effect>
                            <BlurEffect Radius="1"/>
                        </Image.Effect>
                    </Image>
                </Button>
            </StackPanel>-->
        </Grid>

        <Grid Grid.Row="1">
            <TextBlock Text="DarkMilk" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="30" Foreground="White"></TextBlock>
        </Grid>

        <Grid Grid.Row="2">

        </Grid>

        <Grid Grid.Row="3">
            <Button VerticalAlignment="Center" HorizontalAlignment="Center" Width="200" Height="20"  />
        </Grid>
        
    </Grid>
</Window>
